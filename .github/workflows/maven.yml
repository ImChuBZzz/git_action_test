name: Git-Action-Test

on:
  push:
    branches:
      - master

jobs:
  set-up:
    runs-on: ubuntu-latest
    steps:
    - name: "пушим проект"
      uses: actions/checkout@v2
    - name: "запускаем тесты"
      run: mvn -version
